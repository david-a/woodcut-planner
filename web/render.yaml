services:
  - type: web
    name: wood-calculator-frontend
    env: node
    buildCommand: deno run build
    startCommand: node build/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PUBLIC_API_URL
        sync: false # You'll need to set this in Render.com dashboard
    autoDeploy: true
